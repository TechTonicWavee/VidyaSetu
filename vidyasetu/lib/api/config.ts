// The Express backend has been folded into this app's own /api routes (see
// app/api/auth, app/api/directory, app/api/notifications, app/api/teams,
// app/api/invites), so API calls are same-origin now — nothing to configure.
