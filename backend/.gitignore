# Dependencies
/node_modules
.pnpm-store/

# Build output
/dist
/build

# TypeScript
*.tsbuildinfo

# Local env & secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS files
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Antigravity IDE Files
.claude/
agent/skills/
.agents/
skills-lock.json
implementation_plan.md
stash.patch

# Real student PII (names, personal phone numbers, parents' phone numbers,
# emails) — never commit this. Ask whoever maintains the roster for a copy
# to seed a local/dev database instead.
studentsSeed.json

# Real faculty credentials (seedFaculty.ts stores this password column
# unhashed) — keep faculty_seed_template.xlsx (fake example.com rows) as the
# format reference, but never commit a populated data file.
faculty_seed_data.xlsx

# Confidential data and seeding scripts
src/seed/
